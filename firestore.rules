rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Cualquiera puede leer
      allow read: if true;
      // Solo usuarios logueados pueden escribir
      allow write: if request.auth != null;
    }
  }
}